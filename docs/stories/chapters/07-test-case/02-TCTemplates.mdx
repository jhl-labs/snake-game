import { Meta, Source } from '@storybook/blocks';
import { Placeholder } from '../../../components/Placeholder';
import { ChapterNav } from '../../../components/ChapterNav';
import { Mermaid } from '../../../components/Mermaid';

<Meta title="Chapters/07. Test Case 작성 실습/7.2 TC 템플릿" />

# 7.2 TC 템플릿

다양한 상황에 맞는 TC 요청 템플릿을 제공합니다.

---

## TC 유형별 분류

<Mermaid chart={`
flowchart TB
    subgraph Types["TC 유형"]
        Unit["🔹 유닛 테스트<br/>단일 함수"]
        State["🔸 상태 테스트<br/>상태 변경"]
        Error["🔴 에러 테스트<br/>예외 처리"]
        Integration["🔷 통합 테스트<br/>여러 함수"]
    end

    Unit --> Simple["단순한 입출력 검증"]
    State --> Before["변경 전/후 상태 비교"]
    Error --> Edge["경계 조건, NULL, 오버플로우"]
    Integration --> Flow["시나리오 기반 흐름"]
`} />

---

## 기본 함수 테스트

<Source
  language="text"
  code={`
[함수명] 함수에 대한 유닛 테스트를 작성해줘.

테스트 조건:
- 파일: [파일 경로]
- 정상 동작 테스트
- 경계 조건 테스트
- 에러 케이스 테스트

형식:
- AAA 패턴 사용
- assert 매크로 사용
- 각 테스트에 설명 주석
`}
/>

### 예시

<Source
  language="text"
  code={`
snake_init 함수에 대한 유닛 테스트를 작성해줘.

테스트 조건:
- 파일: src/snake.c
- 정상 초기화 (유효한 좌표)
- 경계 조건 (맵 끝 좌표)
- 에러 케이스 (NULL 포인터)

형식:
- AAA 패턴 사용
- assert 매크로 사용
- 각 테스트에 설명 주석
`}
/>

---

## 상태 변경 테스트

<Source
  language="text"
  code={`
[함수명] 함수의 상태 변경을 테스트해줘.

확인할 상태:
- 변경 전 상태: [초기 상태]
- 실행할 동작: [함수 호출]
- 변경 후 상태: [기대 상태]

추가 확인:
- 다른 상태에 영향이 없는지
- 부수 효과가 없는지
`}
/>

### 예시

<Source
  language="text"
  code={`
snake_grow 함수의 상태 변경을 테스트해줘.

확인할 상태:
- 변경 전 상태: length = 3
- 실행할 동작: snake_grow(&snake)
- 변경 후 상태: length = 4

추가 확인:
- 기존 body 배열 위치가 유지되는지
- direction이 변경되지 않는지
`}
/>

---

## 에러 처리 테스트

<Source
  language="text"
  code={`
[함수명] 함수의 에러 처리를 테스트해줘.

테스트할 에러 케이스:
1. NULL 포인터 입력
2. 유효하지 않은 매개변수
3. 경계 초과 값

각 케이스에서 확인할 것:
- 함수 반환값
- 에러 메시지 출력 여부
- 크래시 발생 여부
`}
/>

### 예시

<Source
  language="text"
  code={`
snake_move 함수의 에러 처리를 테스트해줘.

테스트할 에러 케이스:
1. NULL 포인터: snake_move(NULL, DIRECTION_UP)
2. 유효하지 않은 방향: snake_move(&snake, 999)
3. 이미 게임 오버인 상태에서 호출

각 케이스에서 확인할 것:
- false 반환 여부
- 상태 변경이 없는지
- 크래시 없이 처리되는지
`}
/>

---

## 통합 테스트

<Mermaid chart={`
flowchart LR
    subgraph Scenario["통합 테스트 시나리오"]
        Init["1. 초기화<br/>game_init()"]
        Move["2. 이동<br/>snake_move()"]
        Collision["3. 충돌 감지<br/>check_collision()"]
        Grow["4. 성장<br/>snake_grow()"]
        Spawn["5. 음식 생성<br/>food_spawn()"]
    end

    Init --> Move --> Collision --> Grow --> Spawn
    Spawn -.->|"반복"| Move
`} />

<Source
  language="text"
  code={`
[기능/모듈]에 대한 통합 테스트를 작성해줘.

테스트 시나리오:
1. [초기 설정]
2. [동작 1]
3. [동작 2]
4. [최종 상태 확인]

관련 함수:
- [함수 1]
- [함수 2]
- [함수 3]
`}
/>

### 예시

<Source
  language="text"
  code={`
"뱀이 음식을 먹고 성장하는" 시나리오에 대한 통합 테스트를 작성해줘.

테스트 시나리오:
1. 게임 초기화 (뱀 길이 1)
2. 음식 위치에 뱀 이동
3. 충돌 감지 및 음식 섭취 처리
4. 뱀 길이 증가 확인 (길이 2)
5. 새 음식 생성 확인

관련 함수:
- game_init()
- snake_move()
- game_check_food_collision()
- snake_grow()
- food_spawn()
`}
/>

---

## 프로젝트 특화 템플릿

### Snake Game 전용

<Source
  language="text"
  code={`
Snake Game의 [기능]을 테스트해줘.

게임 상태:
- 맵 크기: GRID_WIDTH x GRID_HEIGHT
- 뱀 초기 위치: (x, y)
- 뱀 초기 길이: n

테스트할 동작:
- [동작 설명]

기대 결과:
- [결과 설명]
`}
/>

---

## 핵심 정리

<div style={{
  backgroundColor: '#fef3c7',
  padding: '1rem',
  borderRadius: '8px',
  marginTop: '1.5rem'
}}>

1. **템플릿**을 활용하면 일관된 TC 요청 가능
2. **유닛, 상태 변경, 에러, 통합** 유형별 템플릿
3. 프로젝트에 맞게 **커스터마이징** 가능
4. 구체적인 정보를 채워서 요청

</div>

<ChapterNav
  prevLink="?path=/docs/chapters-07-test-case-작성-실습-7-1-agent로-tc-작성--docs"
  prevTitle="7.1 Agent로 TC 작성"
  nextLink="?path=/docs/chapters-07-test-case-작성-실습-7-3-tc-실습--docs"
  nextTitle="7.3 TC 실습"
/>
